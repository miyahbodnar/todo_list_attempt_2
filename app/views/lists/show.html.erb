<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<div id="items_wrapper">
 <%= render @list.items %>
 <div id="form">
  <%= render "items/form" %>
 </div>
</div>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
